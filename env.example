# ─── Database ──────────────────────────────────────────────
DRIZZLE_DATABASE_URL=postgresql://user:password@host:port/dbname?sslmode=require

# ─── Clerk Auth ─────────────────────────────────────────────
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key
CLERK_SECRET_KEY=sk_test_your_secret_key

NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/

# ─── AI Model (Nano Banana) ─────────────────────────────────
# If you're using the new "nano banana" AI model/service, set its API key here.
# Replace with the provider name and key format your team uses.
NANO_BANANA_API_KEY=your_nano_banana_api_key

# Optional: override the image generation model name used by the server.
# Example: IMAGE_MODEL=gemini-2.0-flash-exp-image-generation
# If you have a different model (e.g. a new 'nano-banana' model name), set it here.
IMAGE_MODEL=gemini-2.0-flash-exp-image-generation


# ─── Vercel Blob Storage ────────────────────────────────────
# Token with read/write access for Vercel Blob storage. In Vercel, you can
# create a Blob Read/Write Token and paste it here as VERCEL_BLOB_TOKEN.
VERCEL_BLOB_TOKEN="your_vercel_blob_read_write_token"